<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Recipe Book</title>
    <style>
        body {
            font-family: 'Arial', sans-serif;
            margin: 0;
            padding: 0;
            background: linear-gradient(120deg, #f6d365, #fda085);
            color: #333;
        }

        h1 {
            text-align: center;
            margin-top: 50px;
            font-size: 2.5rem;
            color: #fff;
        }

        nav {
            text-align: center;
            margin: 20px 0;
        }

        nav a {
            text-decoration: none;
            font-size: 1.2rem;
            margin: 0 15px;
            color: #fff;
            padding: 10px 20px;
            border: 2px solid #fff;
            border-radius: 5px;
            transition: background-color 0.3s, color 0.3s;
        }

        nav a:hover {
            background-color: #fff;
            color: #fda085;
        }

        .recipes-container {
            max-width: 800px;
            margin: 30px auto;
            padding: 20px;
            background: #fff;
            border-radius: 10px;
            box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1);
        }

        .recipe {
            margin-bottom: 20px;
            padding: 15px;
            border-bottom: 1px solid #ccc;
        }

        .recipe:last-child {
            border-bottom: none;
        }

        .recipe h3 {
            margin: 0;
            color: #ff7e5f;
        }

        .recipe p {
            margin: 5px 0;
        }

        @media (max-width: 600px) {
            h1 {
                font-size: 2rem;
            }
            
            nav a {
                font-size: 1rem;
                padding: 8px 15px;
                margin: 0 5px;
            }
        }
    </style>
</head>
<body>
    <h1>Добро пожаловать в Recipe Book!</h1>
    <nav>
        <a href="/register">Регистрация</a>
        <a href="/login">Войти</a>
        <a href="/profile">Профиль</a>
        <a href="/admin">Админ панель</a>
        <a href="/moderator">Модератор панель</a>
    </nav>
    <div class="recipes-container">
        <h2>Все рецепты:</h2>
        <% if (recipes.length > 0) { %>
            <% recipes.forEach(function(recipe) { %>
                <div class="recipe">
                    <h3><%= recipe.name %></h3>
                    <p><strong>Категория:</strong> <%= recipe.category %></p>
                    <p><strong>Описание:</strong> <%= recipe.description %></p>
                    <p><strong>Ингредиенты:</strong> <%= recipe.ingredients %></p>
                    <p><strong>Шаги:</strong> <%= recipe.steps %></p>
                    <p><strong>Дата создания:</strong> <%= new Date(recipe.createdAt).toLocaleDateString() %></p>
                </div>
            <% }); %>
        <% } else { %>
            <p>Пока нет рецептов. Будьте первым, кто добавит рецепт!</p>
        <% } %>
    </div>
</body>
</html>
